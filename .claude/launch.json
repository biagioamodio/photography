{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "photography-main",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["browser-sync", "start", "--server", "--port", "3002", "--files", "*.html, assets/css/*.css, assets/js/*.js, _data/*.json", "--no-notify"],
      "port": 3002
    }
  ]
}
